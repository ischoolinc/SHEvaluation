# 定期評量成績單程式碼流程解析

## 日期：2025年8月12日

### 程式碼位置
檔案：`Program.cs`  
行數：26-36

### 程式碼內容
```csharp
var btn = K12.Presentation.NLDPanels.Student.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["定期評量成績單(測試版)"];
btn.Enable = false;
K12.Presentation.NLDPanels.Student.SelectedSourceChanged += delegate
{
    btn.Enable = Permissions.定期評量成績單權限 && (K12.Presentation.NLDPanels.Student.SelectedSource.Count > 0);
};
btn.Click += new EventHandler(Program_Click);

//權限設定
Catalog permission = RoleAclSource.Instance["學生"]["功能按鈕"];
permission.Add(new RibbonFeature(Permissions.定期評量成績單, "定期評量成績單(測試版)"));
```

### 程式碼流程解析

#### 1. 按鈕初始化
- 從 K12 系統的學生面板中取得「定期評量成績單(測試版)」按鈕
- 按鈕路徑：資料統計 → 報表 → 成績相關報表 → 定期評量成績單(測試版)
- 初始狀態設定為禁用 (`btn.Enable = false`)

#### 2. 動態權限控制
- 監聽學生選擇來源變更事件 (`SelectedSourceChanged`)
- 當學生選擇變更時，動態啟用/禁用按鈕
- 啟用條件：
  - 使用者具有「定期評量成績單」權限 (`Permissions.定期評量成績單權限`)
  - 且已選擇學生 (`K12.Presentation.NLDPanels.Student.SelectedSource.Count > 0`)

#### 3. 按鈕點擊事件
- 綁定按鈕點擊事件到 `Program_Click` 方法
- 當使用者點擊按鈕時，執行對應的功能

#### 4. 權限系統整合
- 取得學生功能按鈕的權限目錄
- 新增「定期評量成績單」功能到權限系統
- 權限識別碼：`定期評量成績單.D04E7F02-89C1-4412-81FA-8D87B96BF847`

### 權限檢查機制
- 使用 `FISCA.Permission.UserAcl.Current[定期評量成績單].Executable` 檢查使用者權限
- 權限與學生選擇狀態結合，確保功能按鈕的適當啟用/禁用

### 設計特點
1. **安全性**：雙重檢查（權限 + 學生選擇）
2. **使用者體驗**：動態按鈕狀態，避免無效操作
3. **整合性**：與 K12 系統的學生管理面板深度整合
4. **權限管理**：完整的權限控制機制

---

## Todo 清單

### Todo 1: 功能路徑調整 - 新增「即將下架報表」層級

#### 調整內容
- **原路徑**：`["資料統計"]["報表"]["成績相關報表"]["定期評量成績單(測試版)"]`
- **新路徑**：`["資料統計"]["報表"]["成績相關報表"]["即將下架報表"]["定期評量成績單(測試版)"]`

#### 需要修改的程式碼位置
- 檔案：`Program.cs`
- 行數：26
- 修改項目：按鈕路徑定義

#### 修改方式
```csharp
// 原本的程式碼
var btn = K12.Presentation.NLDPanels.Student.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["定期評量成績單(測試版)"];

// 修改後的程式碼
var btn = K12.Presentation.NLDPanels.Student.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["即將下架報表"]["定期評量成績單(測試版)"];
```

#### 注意事項
1. 確保 K12 系統中已存在「即將下架報表」這個功能分類
2. 測試修改後的功能按鈕是否能正常顯示和運作
3. 確認權限設定是否需要相應調整
4. 建議在測試環境中先進行驗證

#### 修改執行記錄
- **執行日期**：2025年8月12日
- **執行狀態**：✅ 已完成
- **修改檔案**：`Program.cs`
- **修改行數**：26
- **修改內容**：成功新增「即將下架報表」層級到按鈕路徑
- **修改前路徑**：`["資料統計"]["報表"]["成績相關報表"]["定期評量成績單(測試版)"]`
- **修改後路徑**：`["資料統計"]["報表"]["成績相關報表"]["即將下架報表"]["定期評量成績單(測試版)"]`
- **執行結果**：程式碼已成功更新，功能按鈕路徑已調整完成
