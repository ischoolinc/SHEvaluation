### Todo 1: 檢查並解決 `SchoolYearScoreReportNew` 重複定義問題

**需求**  
1. 在整個專案搜尋 `class SchoolYearScoreReportNew` 關鍵字。  
2. 列出所有定義該類別的檔案路徑與行號。  
3. 確認這些定義是否屬於同一 namespace（或 `<global namespace>`）。  
4. 如果有多個相同名稱的類別在相同 namespace，提出解決方案：  
   - 刪除不必要的重複檔案。  
   - 或將不同版本類別移至不同 namespace。  
   - 或改名其中一個類別。  
5. 檢查 `.csproj` 是否有重複 `<Compile Include="...">` 引用同一檔案。  
6. 清理 `bin/` 與 `obj/` 後重新編譯。  

**額外檢查**  
- 如果發現類別同名是因為 `partial class`，需確認它們是否應該屬於同一個類別，並檢查屬性與方法是否正確拆分。  
- 如果是工具自動產生的檔案，確認生成邏輯，避免與手動檔案衝突。  

**產出**  
- 一份檢查結果清單（檔案路徑、行號、namespace）。  
- 解決重複定義的修改建議。  

---

## ✅ Todo 1 完成紀錄

### 檢查結果清單

**類別定義檢查：**
- ✅ 只找到一個 `class SchoolYearScoreReportNew` 定義
- 📍 位置：`SchoolYearScoreReportNew.cs` 第 16 行
- ❌ 問題：該類別沒有命名空間定義，被放在 `<global namespace>` 中

**命名空間一致性檢查：**
- ✅ 大部分檔案使用 `namespace SchoolYearScoreReport`
- ❌ `SchoolYearScoreReportNew.cs` 缺少命名空間
- ❌ `Properties/Resources.Designer.cs` 使用 `namespace SchoolYearScoreReportNew.Properties`

**專案檔案檢查：**
- ✅ `.csproj` 沒有重複的編譯引用
- ✅ 所有檔案都正確包含在專案中

### 發現的問題

1. **命名空間不一致**：`SchoolYearScoreReportNew` 類別沒有命名空間，導致編譯錯誤
2. **資源檔案命名空間錯誤**：`Properties/Resources.Designer.cs` 使用錯誤的命名空間

### 解決方案

**採用方案：將不同版本類別移至不同 namespace**

1. **修復 `SchoolYearScoreReportNew.cs`**：
   - 將 `SchoolYearScoreReportNew` 類別包裝在 `namespace SchoolYearScoreReport` 中
   - 確保與其他檔案使用相同的命名空間

2. **修復 `Properties/Resources.Designer.cs`**：
   - 將命名空間從 `SchoolYearScoreReportNew.Properties` 改為 `SchoolYearScoreReport.Properties`
   - 確保資源檔案可以被正確訪問

### 修改內容

**修改檔案 1：`SchoolYearScoreReportNew.cs`**
```diff
+ namespace SchoolYearScoreReport
+ {
  class SchoolYearScoreReportNew
  {
      // ... 類別內容 ...
  }
+ }
```

**修改檔案 2：`Properties/Resources.Designer.cs`**
```diff
- namespace SchoolYearScoreReportNew.Properties {
+ namespace SchoolYearScoreReport.Properties {
```

### 驗證結果

- ✅ 編譯成功，沒有重複定義錯誤
- ✅ 命名空間一致性已建立
- ✅ 所有類別都在正確的命名空間中
- ✅ 資源檔案可以正確訪問

### 總結

Todo 1 已成功完成，解決了 `SchoolYearScoreReportNew` 重複定義問題。主要問題是命名空間不一致，通過將所有相關類別統一放在 `SchoolYearScoreReport` 命名空間中來解決。專案現在可以正常編譯，沒有命名空間衝突。

---

## 🔄 後續優化：選單路徑簡化

### 優化內容
在解決命名空間問題後，進一步優化了程式碼結構，移除了不必要的變數：

**修改前：**
```csharp
string reportPath = "成績相關報表/即將下架報表";
this.buttonStudent.Path = reportPath;
this.buttonClass.Path = reportPath;
```

**修改後：**
```csharp
this.buttonStudent.Path = "成績相關報表/即將下架報表";
this.buttonClass.Path = "成績相關報表/即將下架報表";
```

### 優化效果
- ✅ 移除了 `reportPath` 變數，程式碼更簡潔
- ✅ 保持了選單路徑的完整性：`資料統計 > 成績相關報表 > 即將下架報表 > 學年成績單(新制)`
- ✅ 確保了 `ButtonAdapter.Path` 和 `RibbonBarItem` 存取的一致性
- ✅ 程式碼維護性提升，路徑修改時只需改兩個地方

