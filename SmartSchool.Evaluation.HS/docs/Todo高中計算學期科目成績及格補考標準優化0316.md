# Todo高中計算學期科目成績及格補考標準優化0316.md

## 目標

在 `C:\ischoolProject\SHEvaluation\SmartSchool.Evaluation.HS\WearyDogComputer.cs` 針對 `FillSemesterSubjectCalcScore` 進行**及格標準 / 補考標準寫入優化**。

本次重點不是重做整支流程，而是：

1. 修正 `修課及格標準` 寫入時機不一致問題
2. 修正 `修課補考標準` 寫入時機不一致問題
3. 避免只在特定 if 條件下才寫入
4. 避免先用舊 XML 值判斷，再寫入新值
5. 讓新增 / 更新 / 重修 / 補修 / 再次修習分支都使用一致寫法

修改完成後，請將處理內容記錄在：

`高中計算學期科目成績調整0316.md`

---

## 問題摘要

目前 Cursor 修改後版本已加入：

* `GetEffectiveMakeupStandard(...)`
* `SetPassingStandardAttribute(...)`
* `SetMakeupStandardAttribute(...)`

方向正確，但仍有以下風險：

### 1. 寫入時機不一致

有些分支會先做：

* `是否取得學分`
* `ScoreP`
* `RepeatScoreP`
* `ReScoreP`
* `ReAScoreP`

之後才寫 `修課及格標準` / `修課補考標準`

這會造成：

* 判斷使用舊 XML 值
* 最後 XML 寫的是新值
* 結果與判斷可能不一致

---

### 2. 仍有分支依賴條件式寫入

雖然部分地方已改成統一寫入，但仍需全面檢查：

* 不可只因為 `studentFinalScoreDict.ContainsKey(...)`
* 或只有進入某個區塊時
* 才更新 `修課及格標準` / `修課補考標準`

必須做到：

> 只要該筆 Subject 有被處理，就一定寫入這兩個欄位。

---

### 3. passscore/effectivePassscore 混用

目前很多地方先算：

```csharp
decimal effectivePassscore = ...
```

後面又重新算：

```csharp
decimal passscore = 100;
...
```

這樣容易造成：

* 寫入 XML 用 A
* 判斷是否取得學分用 B
* 最後不一致

---

### 4. 補考標準應明確清空，不可殘留舊值

若 DB 沒補考標準：

* XML 應寫成空字串
* 不可保留舊 XML 的 `修課補考標準`

這點 helper 已做，但要確認所有分支都有呼叫。

---

## 本次修改原則

1. **不要大改商業規則**
2. **不要重構整支函式**
3. 只做「寫入標準的統一化」
4. `修課及格標準`、`修課補考標準`：

   * 一律先算
   * 一律先寫
   * 再做後續判斷
5. 盡量減少重複程式碼
6. 讓 Cursor 修改風險低、容易驗證

---

## 一、先補 helper method（若尚未存在）

如果目前版本還沒有，請補齊以下方法；若已存在，請確認命名與行為一致。

---

### 1. 取得有效及格標準

```csharp
private decimal GetEffectivePassingStandard(
    string studentId,
    string key,
    int gradeYear,
    Dictionary<string, Dictionary<string, decimal>> studentPassScoreDict,
    Dictionary<int, decimal> applyLimit)
{
    decimal passscore = 60;

    if (studentPassScoreDict != null &&
        studentPassScoreDict.ContainsKey(studentId) &&
        studentPassScoreDict[studentId].ContainsKey(key))
    {
        decimal value = studentPassScoreDict[studentId][key];
        if (value != -1)
            return value;
    }

    if (applyLimit != null && applyLimit.ContainsKey(gradeYear))
        return applyLimit[gradeYear];

    return passscore;
}
```

---

### 2. 取得有效補考標準

```csharp
private decimal? GetEffectiveMakeupStandard(
    string studentId,
    string key,
    Dictionary<string, Dictionary<string, decimal?>> studentMakeupScoreDict)
{
    if (studentMakeupScoreDict != null &&
        studentMakeupScoreDict.ContainsKey(studentId) &&
        studentMakeupScoreDict[studentId].ContainsKey(key))
    {
        return studentMakeupScoreDict[studentId][key];
    }

    return null;
}
```

---

### 3. 統一寫入及格標準

```csharp
private void SetPassingStandardAttribute(
    XmlElement scoreElement,
    decimal passscore,
    int decimals,
    RoundMode mode)
{
    if (scoreElement == null)
        return;

    scoreElement.SetAttribute("修課及格標準", GetRoundScore(passscore, decimals, mode).ToString());
}
```

---

### 4. 統一寫入補考標準

```csharp
private void SetMakeupStandardAttribute(
    XmlElement scoreElement,
    decimal? makeupscore,
    int decimals,
    RoundMode mode)
{
    if (scoreElement == null)
        return;

    if (makeupscore.HasValue)
        scoreElement.SetAttribute("修課補考標準", GetRoundScore(makeupscore.Value, decimals, mode).ToString());
    else
        scoreElement.SetAttribute("修課補考標準", "");
}
```

---

### 5. 建議新增：統一寫入兩個標準

為了減少重複，請新增一個 helper：

```csharp
private void ApplyCourseStandards(
    XmlElement scoreElement,
    string studentId,
    string key,
    int gradeYear,
    Dictionary<string, Dictionary<string, decimal>> studentPassScoreDict,
    Dictionary<string, Dictionary<string, decimal?>> studentMakeupScoreDict,
    Dictionary<int, decimal> applyLimit,
    int decimals,
    RoundMode mode)
{
    decimal effectivePassscore = GetEffectivePassingStandard(
        studentId,
        key,
        gradeYear,
        studentPassScoreDict,
        applyLimit);

    decimal? effectiveMakeupscore = GetEffectiveMakeupStandard(
        studentId,
        key,
        studentMakeupScoreDict);

    SetPassingStandardAttribute(scoreElement, effectivePassscore, decimals, mode);
    SetMakeupStandardAttribute(scoreElement, effectiveMakeupscore, decimals, mode);
}
```

---

## 二、整理 dtFinalScore 區塊

請確認 `dtFinalScore` 迴圈內：

### 必須有

1. `studentPassScoreDict`
2. `studentMakeupScoreDict`
3. `studentHasFinalScoreDict`
4. `studentFinalScoreDict`

### 補考標準整理規則

* 有值就存 decimal
* 空值就存 null
* 不可因為沒值就不建立 key，避免後續判斷不一致

---

## 三、修改原則：先寫標準，再做任何判斷

以下所有情境都要遵守：

### 正確順序

```csharp
1. 建立 / 取得 scoreElement
2. 更新課程基本欄位
3. 呼叫 ApplyCourseStandards(...)
4. 再做 原始成績 / 指定總成績 / 是否取得學分 / ScoreP / ReScoreP / ReAScoreP 判斷
```

### 禁止順序

```csharp
1. 先從 XML 讀 修課及格標準 做判斷
2. 最後才寫新標準
```

---

## 四、需要逐一處理的分支

以下分支都要檢查並統一：

---

### A. 再次修習 → 更新既有成績

找到類似：

```csharp
XmlElement updateScoreElement = updateScoreInfo.Detail;
```

在更新基本欄位後，立即補：

```csharp
ApplyCourseStandards(
    updateScoreElement,
    var.StudentID,
    key,
    updateScoreInfo.GradeYear,
    studentPassScoreDict,
    studentMakeupScoreDict,
    applyLimit,
    decimals,
    mode);
```

### 要求

* 這段之後，所有 `maxScore`、`是否取得學分`、`ScoreP` 判斷都以最新標準為準
* 不要再重複另外算 `effectivePassscore` / `effectiveMakeupscore`
* 不要再從 `studentFinalScoreDict` 裡重複設定 `修課及格標準` / `修課補考標準`

---

### B. 再次修習 → 新增成績

找到類似：

```csharp
XmlElement newScoreInfo = doc.CreateElement("Subject");
```

在基本欄位、指定學年科目名稱之後，立即補：

```csharp
ApplyCourseStandards(
    newScoreInfo,
    var.StudentID,
    key,
    gradeYear.HasValue ? gradeYear.Value : 0,
    studentPassScoreDict,
    studentMakeupScoreDict,
    applyLimit,
    decimals,
    mode);
```

---

### C. 重修成績 → 寫回原學期

找到：

```csharp
XmlElement updateScoreElement = previousSubjectScoreInfo.Detail;
```

在算出 `passscore` 後，改成不要分開寫兩行，直接：

```csharp
ApplyCourseStandards(
    updateScoreElement,
    var.StudentID,
    key,
    previousSubjectScoreInfo.GradeYear,
    studentPassScoreDict,
    studentMakeupScoreDict,
    applyLimit,
    decimals,
    mode);
```

### 另外要求

後續 `是否取得學分`、`ScoreP`、`ReAScoreP` 等判斷不要再依賴舊值。

---

### D. 補修成績

找到：

```csharp
makeUpScoreInfo.Detail
```

在算出 `passscore` 後立即補：

```csharp
ApplyCourseStandards(
    makeUpScoreInfo.Detail,
    var.StudentID,
    key,
    makeUpScoreInfo.GradeYear,
    studentPassScoreDict,
    studentMakeupScoreDict,
    applyLimit,
    decimals,
    mode);
```

### 注意

這一段後面有：

* `ScoreP`
* `reScore`
* `reScoreP`

這些都必須在寫入最新標準後才做。

---

### E. 一般修課 / 更新既有成績

找到：

```csharp
XmlElement updateScoreElement = updateScoreInfo.Detail;
```

在基本欄位更新完後立即補：

```csharp
ApplyCourseStandards(
    updateScoreElement,
    var.StudentID,
    key,
    updateScoreInfo.GradeYear,
    studentPassScoreDict,
    studentMakeupScoreDict,
    applyLimit,
    decimals,
    mode);
```

這是本次最重要的分支，請優先確認。

---

### F. 一般修課 / 新增成績

找到：

```csharp
XmlElement newScoreInfo = doc.CreateElement("Subject");
```

在基本欄位與指定學年科目名稱處理後立即補：

```csharp
ApplyCourseStandards(
    newScoreInfo,
    var.StudentID,
    key,
    gradeYear.HasValue ? gradeYear.Value : 0,
    studentPassScoreDict,
    studentMakeupScoreDict,
    applyLimit,
    decimals,
    mode);
```

---

## 五、移除重複且危險的舊寫法

請搜尋以下關鍵字並調整：

* `SetAttribute("修課及格標準"`
* `SetAttribute("修課補考標準"`

### 原則

除了 helper method 內，其他地方不要再直接手動設定這兩個欄位。

### 目標

讓整支程式只透過：

* `ApplyCourseStandards(...)`
* `SetPassingStandardAttribute(...)`
* `SetMakeupStandardAttribute(...)`

來更新這兩個欄位。

---

## 六、統一 passscore 使用方式

目前很多地方有這種重複邏輯：

```csharp
decimal passscore = 100;
if (studentPassScoreDict.ContainsKey(...))
{
    ...
}
```

請調整為：

### 建議方式

在需要 numeric passscore 做判斷時，直接統一：

```csharp
decimal passscore = GetEffectivePassingStandard(
    var.StudentID,
    key,
    對應年級,
    studentPassScoreDict,
    applyLimit);
```

### 目的

避免：

* XML 寫的是 A
* 後續判斷用的是 B

---

## 七、禁止再從 XML 讀舊值來當主來源

請搜尋這類寫法：

```csharp
decimal.TryParse(scoreElement.GetAttribute("修課及格標準"), out ...)
```

### 處理原則

* 可以在最後做防呆
* 但主要邏輯不要依賴 XML 舊值
* 主來源應是：

```csharp
GetEffectivePassingStandard(...)
GetEffectiveMakeupStandard(...)
```

---

## 八、效能順手優化（本次可一併做）

這次不要求大重構，但有兩個低風險優化可以做：

---

### 1. sidList 改 HashSet

目前：

```csharp
List<string> sidList = new List<string>();

foreach (StudentRecord sr in students)
{
    if (!sidList.Contains(sr.StudentID))
        sidList.Add(sr.StudentID);
}
```

改成：

```csharp
HashSet<string> sidSet = new HashSet<string>();

foreach (StudentRecord sr in students)
{
    if (!string.IsNullOrWhiteSpace(sr.StudentID))
        sidSet.Add(sr.StudentID);
}

List<string> sidList = sidSet.ToList();
```

### 原因

`List.Contains` 是 O(n)，HashSet 比較穩。

---

### 2. currentSubjectScoreList 取值可考慮後續優化

目前常用：

```csharp
foreach (SemesterSubjectScoreInfo s in currentSubjectScoreList.Keys)
{
    if (currentSubjectScoreList[s] == key)
```

這是 O(n) 搜索。

本次若不想大改可先不動，但請在記錄文件寫成後續優化建議。

---

## 九、驗證案例

至少驗證以下情境：

---

### 測試 1：一般修課 / 更新模式

1. 既有 Subject 已存在
2. 修改 `sc_attend.passing_standard`
3. 修改 `sc_attend.makeup_standard`
4. 重算
5. 確認：

   * `修課及格標準` 更新
   * `修課補考標準` 更新
   * `是否取得學分` 依新標準判斷

---

### 測試 2：一般修課 / 更新模式 / 補考標準改空

1. 原 XML 有 `修課補考標準`
2. DB 的 `makeup_standard` 改空
3. 重算
4. 確認 XML 的 `修課補考標準` 變成空字串，不殘留舊值

---

### 測試 3：一般修課 / 新增模式

1. 原本無 Subject
2. DB 有及格 / 補考標準
3. 重算
4. 確認新增的 Subject 兩個欄位都有寫

---

### 測試 4：重修成績

1. 舊學期已有成績
2. 本學期規則為 `重修成績`
3. 重算後確認：

   * `重修成績` 正常
   * `修課及格標準` 正常
   * `修課補考標準` 正常

---

### 測試 5：補修成績

1. 本次補修回寫舊 Subject
2. 確認：

   * `修課及格標準` 正常
   * `修課補考標準` 正常
   * `ScoreP / ReScoreP` 使用最新值

---

### 測試 6：再次修習

1. 有相同科目級別歷史資料
2. 規則為 `再次修習`
3. 檢查更新與新增兩條路徑都正常

---

### 測試 7：無 passing_standard，但有 applyLimit

1. DB 無 passing_standard
2. 規則內 applyLimit 有年級標準
3. 確認 XML `修課及格標準` 使用 fallback 值

---

## 十、記錄文件

修改完成後，請新增或更新：

`高中計算學期科目成績調整0316.md`

內容至少包含：

1. 問題描述
2. 原始風險
3. 修改原則
4. 新增 helper method
5. 調整分支
6. 驗證案例
7. 驗證結果
8. 效能觀察
9. 後續可再優化項目

---

## 十一、回報要求

完成後請回報：

1. 是否新增 `ApplyCourseStandards(...)`
2. 哪些分支已改成統一寫入
3. 是否已移除散落的 `SetAttribute("修課及格標準"` / `SetAttribute("修課補考標準"` 寫法
4. 是否已改成先寫標準再判斷
5. 測試結果
6. 是否還有殘留風險

---

## 補充提醒

### 不要做的事

1. 不要改壞 `designate_final_score` 邏輯
2. 不要更動重修 / 補修 / 再次修習核心商業規則
3. 不要把及格標準硬塞成固定 60
4. 不要只修一般修課
5. 不要讓 XML 還有殘留舊補考標準的可能

### 本次最重要目標

**讓 `修課及格標準` 與 `修課補考標準` 的寫入時機與來源完全一致化，並先寫入再做判斷。**
