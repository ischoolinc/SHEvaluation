# 即將下架報表路徑調整 - 0811

## 調整概述

將學期成績單報表從原本的路徑調整到"成績相關報表"下的"即將下架報表"分類中。

## 調整前後對照

### 調整前
```csharp
string path = "成績單列印管理";  // 原本的路徑
```

### 調整後
```csharp
string path = "成績相關報表/即將下架報表";  // 新的路徑
```

## 修改檔案

**檔案路徑**: `Reports/SemesterScoreReport/SemesterScoreReport.cs`

**修改行數**: 第32行

**修改內容**:
```32:43:Reports/SemesterScoreReport/SemesterScoreReport.cs
// ... existing code ...
            string reportName = "學期成績單";
            string path = "成績相關報表/即將下架報表";  // 修改這裡

            _ErrorMessage = new StringBuilder();

            button = new SecureButtonAdapter("Report0050");
            button.Text = reportName;
            button.Path = path;
            button.OnClick += new EventHandler(button_OnClick);
            StudentReport.AddReport(button);

            button2 = new SecureButtonAdapter("Report0150");
            button.Text = reportName;
            button2.Path = path;
            button2.OnClick += new EventHandler(button2_OnClick);
            ClassReport.AddReport(button2);
// ... existing code ...
```

## 路徑結構說明

### 調整後的路徑層級
1. **第一層**: 成績相關報表
2. **第二層**: 即將下架報表  
3. **第三層**: 學期成績單

### 完整路徑
```
主選單 → 成績相關報表 → 即將下架報表 → 學期成績單
```

## 影響範圍

### 受影響的報表按鈕
- **學生報表按鈕** (Report0050)
- **班級報表按鈕** (Report0150)

### 權限代碼
- `Report0050` - 學生學期成績單報表
- `Report0150` - 班級學期成績單報表

## 技術細節

### 路徑分隔符
- 使用 `/` 作為路徑層級分隔符
- 符合系統的路徑命名規範

### 變數作用域
- `path` 變數同時影響兩個按鈕的路徑設定
- 確保學生和班級報表路徑一致

## 注意事項

1. **權限設定**: 路徑變更後，可能需要調整相關的權限設定
2. **一致性檢查**: 建議檢查其他相關報表，保持路徑結構的一致性
3. **測試驗證**: 修改後需要測試確認報表能正確顯示在新的路徑位置
4. **用戶體驗**: 路徑調整可能影響用戶的使用習慣，需要適當的用戶通知

## 相關檔案

- `Reports/SemesterScoreReport/SemesterScoreReport.cs` - 主要修改檔案
- `Reports/SemesterScoreReport/SemesterScoreReportForm.cs` - 報表設定表單
- `Reports/SemesterScoreReport/SemesterScoreReportConfig.cs` - 報表配置設定

## 修改日期

**修改日期**: 2025年8月11日

**修改人員**: [請填入修改人員姓名]

**修改原因**: 將學期成績單報表歸類到即將下架報表分類中

## 後續建議

1. **路徑標準化**: 建立統一的報表路徑命名規範
2. **文檔更新**: 更新相關的用戶手冊和操作指南
3. **培訓通知**: 通知相關用戶路徑調整的變更
4. **監控追蹤**: 追蹤用戶對新路徑的適應情況
