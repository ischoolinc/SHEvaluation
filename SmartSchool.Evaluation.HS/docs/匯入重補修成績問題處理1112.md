Todo 1:
幫檢查 ImportExport\ImportMakeupRetakeScore.cs，HisClassName = studentRec.RefClass.ClassName;
當 studentRec.RefClass == null 是否有處理?

✅ 已完成檢查與修正：
- 發現兩處使用 `studentRec.RefClass.ClassName` 的地方（第694行和第935行）
- 兩處都缺少 null 檢查，可能導致 NullReferenceException
- 已修正：在存取 `studentRec.RefClass.ClassName` 前加入 `if (studentRec.RefClass != null)` 檢查
- 修正後已通過 linter 檢查，無錯誤

Todo 2:
我依照 Todo 1 檢查結果，修改 ImportMakeupRetakeScore.cs 程式，修改完成後寫入匯入重補修成績問題處理1112.md。

✅ 已完成修改：
- 已依照 Todo 1 的檢查結果，在 ImportMakeupRetakeScore.cs 中修正兩處問題
- 修正位置1：第693-697行（補修成績處理區塊）
- 修正位置2：第937-941行（重修成績處理區塊）
- 修正內容：在存取 `studentRec.RefClass.ClassName` 前加入 `if (studentRec.RefClass != null)` 檢查
- 修正後的程式碼已通過 linter 檢查，無錯誤
- 修正方案能有效避免當 `studentRec.RefClass` 為 null 時產生的 NullReferenceException