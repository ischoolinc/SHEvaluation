# Cursor Todo產生學習歷程重讀判斷調整0319.md
# 檔名：產生學習歷程重讀判斷調整0319.md

## 🎯 目標
在 StudentLearningHistoryProcessor.cs 中，調整 5.3「重讀成績」邏輯：

1. 讀取：
   - 再次修習成績（dataValueRecord.Score）
   - 補考成績（matchingSemsScore.ReScore）
   - 原始成績（matchingSemsScore.Score）

2. 取最大值 → 回寫：
   - 重讀成績（RepeatScore）

3. 使用最大值與修課及格標準（matchingSemsScore.ScoreP）比較：
   - 回寫 成績及格（RepeatScoreP）

⚠️ 注意：
- 不可修改原有 ReScore / ReScoreP 邏輯
- 僅新增，不可影響既有流程

---

## 📍 修改檔案
StudentLearningHistoryProcessor.cs

---

## 📍 修改位置
搜尋關鍵字：

dataValueRecord.ReScoreP

找到「補考及格計算完成」的區塊（在 5.3 處理流程內）

👉 在該區塊 **之後** 新增程式碼

---

## 🛠️ 新增程式碼

```csharp
// ===== 新增：重讀成績 / 成績及格 判斷 =====

// 收集三種成績來源
List<decimal> scoreCandidates = new List<decimal>();

decimal tmpScore;

// 再次修習成績（5.3 原資料）
if (decimal.TryParse(dataValueRecord.Score, out tmpScore))
{
    scoreCandidates.Add(tmpScore);
}

// 補考成績（學期成績）
if (decimal.TryParse(matchingSemsScore.ReScore, out tmpScore))
{
    scoreCandidates.Add(tmpScore);
}

// 原始成績（學期成績）
if (decimal.TryParse(matchingSemsScore.Score, out tmpScore))
{
    scoreCandidates.Add(tmpScore);
}

// 取最大值
if (scoreCandidates.Count > 0)
{
    decimal maxScore = scoreCandidates.Max();

    // 回寫重讀成績
    dataValueRecord.RepeatScore = maxScore.ToString();

    // 取得及格標準（預設 60）
    decimal passStandard = 60;

    if (!string.IsNullOrWhiteSpace(matchingSemsScore.ScoreP))
    {
        decimal.TryParse(matchingSemsScore.ScoreP, out passStandard);
    }

    // 判斷成績及格
    dataValueRecord.RepeatScoreP = maxScore >= passStandard ? "1" : "0";
}
else
{
    dataValueRecord.RepeatScore = "";
    dataValueRecord.RepeatScoreP = "-1";
}
````

---

## ⚠️ 必要前置檢查

請確認以下兩點：

1️⃣ SubjectScoreRec108 類別需存在：

* RepeatScore
* RepeatScoreP

若沒有，需補上：

```csharp
public string RepeatScore { get; set; }
public string RepeatScoreP { get; set; }
```

2️⃣ SaveScores53(...) 必須有寫入：

* 重讀成績
* 成績及格

否則本次新增不會寫入資料庫

---

## ✅ 驗證方式

請準備測試資料：

情境1：

* 再次修習成績 = 70
* 補考成績 = 65
* 原始成績 = 60
  👉 預期：
* RepeatScore = 70
* RepeatScoreP = 1

情境2：

* 再次修習成績 = 50
* 補考成績 = 55
* 原始成績 = 58
  👉 預期：
* RepeatScore = 58
* RepeatScoreP = 0（若及格標準60）

情境3：

* 全部無值
  👉 預期：
* RepeatScore = ""
* RepeatScoreP = -1

---

## 🧾 完成紀錄

修改完成時間：2026-03-19

調整內容：

* 新增「重讀成績」最大值計算邏輯
* 新增「成績及格」判斷
* 不影響既有補考邏輯

影響範圍：

* 僅 5.3 重讀成績計算
* 不影響 4.2 / 6.2
