# Todo.md

## 目標
在 `WearyDogComputer.cs` 的 `FillSemesterSubjectCalcScore` 中，加入以下規則：

> 當設定 **重修成績** 時，若 **修課直接指定總成績** 有值，則應以該值作為重修成績來源；  
> 但回寫到 `重修成績` 時，仍需受 **及格標準** 限制。  
> 例如：修課直接指定總成績 = 100，及格標準 = 60，則回寫 `重修成績 = 60`。

並且修改完成後，將處理內容記錄到：

- `計算學期科目成績調整0306.md`

---

## 限制條件
1. 只修改 `C:\ischoolProject\SHEvaluation\SmartSchool.Evaluation.HS\WearyDogComputer.cs`
2. 不要更動其他檔案邏輯
3. 維持現有「課程成績 `sacRecord.FinalScore` 也可寫入重修成績」的行為
4. 新增規則後，必須支援：
   - 課程成績 100、及格標準 60 → 回寫重修成績 60
   - 修課直接指定總成績 100、及格標準 60 → 回寫重修成績 60
5. 若同時存在：
   - `sacRecord.FinalScore`
   - `修課直接指定總成績`

   請以 **修課直接指定總成績優先** 作為回寫 `重修成績` 的來源
6. 回寫 `重修成績` 時仍需保留目前邏輯：
   - 新分數若低於既有 `重修成績`，不要覆蓋
   - 新分數若高於既有 `重修成績`，才更新
   - 寫入前需先套用及格標準上限

---

## 要修改的位置
請在 `FillSemesterSubjectCalcScore` 內，找到：

- `else if (ruleType == "重修成績")`

在這個分支中，目前有這段邏輯：

```csharp
decimal roundedScore = GetRoundScore(sacRecord.FinalScore, decimals, mode);
```

目前 `重修成績` 的來源只使用 `sacRecord.FinalScore`。  
要改成：

- 先判斷此科目是否有 `修課直接指定總成績`
- 若有，優先取 `designate_final_score`
- 若無，才使用 `sacRecord.FinalScore`

---

## 實作要求

### 1. 新增重修成績來源判斷
在 `ruleType == "重修成績"` 分支中，建立一個新的來源分數，例如：

```csharp
decimal sourceScore;
```

判斷順序：

1. 先檢查 `studentFinalScoreDict` 是否包含：
   - `var.StudentID`
   - `key`（科目 + 級別）
2. 若有，取出：
   - `designate_final_score`
3. 若 `designate_final_score` 可轉成 decimal，則：
   - `sourceScore = GetRoundScore(designate_final_score, decimals, mode)`
4. 否則：
   - `sourceScore = GetRoundScore(sacRecord.FinalScore, decimals, mode)`

---

### 2. 套用及格標準上限
沿用目前 `passscore` 邏輯：

- 若 `sourceScore > passscore`
- 則實際寫回 `重修成績 = passscore`
- 否則寫回 `sourceScore`

---

### 3. 保留既有高分覆蓋低分機制
目前有這段：

```csharp
decimal previousScore;
if (decimal.TryParse(updateScoreElement.GetAttribute("重修成績"), out previousScore))
{
    if (roundedScore >= previousScore)
    {
        if (roundedScore > passscore)
            roundedScore = passscore;
        updateScoreElement.SetAttribute("重修成績", "" + roundedScore);
    }
}
else
{
    if (roundedScore > passscore)
        roundedScore = passscore;

    updateScoreElement.SetAttribute("重修成績", "" + roundedScore);
}
```

請改成使用新的來源分數變數，但保留原本行為：

- 舊值存在時，只在新值 >= 舊值時更新
- 更新前仍要先套及格標準上限

---

### 4. 明確處理指定總成績優先
請讓程式行為符合下列案例：

#### 案例 A
- 規則：重修成績
- 課程成績：100
- 修課直接指定總成績：空
- 及格標準：60

預期：
- 回寫 `重修成績 = 60`

#### 案例 B
- 規則：重修成績
- 課程成績：40
- 修課直接指定總成績：100
- 及格標準：60

預期：
- 回寫 `重修成績 = 60`

#### 案例 C
- 規則：重修成績
- 課程成績：80
- 修課直接指定總成績：50
- 及格標準：60

預期：
- 回寫 `重修成績 = 50`

#### 案例 D
- 規則：重修成績
- 原本 `重修成績 = 60`
- 本次來源分數（指定總成績或課程成績處理後）= 55

預期：
- 不覆蓋，維持 `重修成績 = 60`

---

## 建議修改方式
建議把 `重修成績` 分支中的來源值改成類似以下結構：

```csharp
decimal sourceScore = 0m;
bool hasSourceScore = false;

// 優先取修課直接指定總成績
if (studentFinalScoreDict.ContainsKey(var.StudentID) &&
    studentFinalScoreDict[var.StudentID].ContainsKey(key))
{
    DataRow dr = studentFinalScoreDict[var.StudentID][key];
    decimal designateScore;
    if (dr["designate_final_score"] != null &&
        decimal.TryParse(dr["designate_final_score"].ToString(), out designateScore))
    {
        sourceScore = GetRoundScore(designateScore, decimals, mode);
        hasSourceScore = true;
    }
}

// 沒有指定總成績時，退回課程成績
if (!hasSourceScore && sacRecord.HasFinalScore)
{
    sourceScore = GetRoundScore(sacRecord.FinalScore, decimals, mode);
    hasSourceScore = true;
}
```

然後再把 `sourceScore` 套用：
- 及格標準上限
- 與舊 `重修成績` 比較
- 寫回 `updateScoreElement.SetAttribute("重修成績", ...)`

> 可依現有程式風格微調，不必逐字照抄，但行為必須一致。

---

## 驗證重點
修改後請自行檢查：

1. `重修成績` 分支是否已支援 `修課直接指定總成績`
2. `修課直接指定總成績` 是否優先於 `sacRecord.FinalScore`
3. 寫回 `重修成績` 是否仍受及格標準限制
4. 是否保留原本「高分才覆蓋低分」機制
5. 是否未影響：
   - `再次修習`
   - `補修成績`
   - 一般修課
6. 編譯是否通過，避免引入：
   - 變數名稱衝突
   - scope 錯誤
   - `key` / `studentSubjectKey` 使用錯誤

---

## 修改完成後
請新增或更新紀錄檔：

- `計算學期科目成績調整0306.md`

內容至少包含：

1. 修改目的
2. 修改檔案
3. 調整前行為
4. 調整後行為
5. 驗證案例摘要

---

## 調整紀錄範本
可在 `計算學期科目成績調整0306.md` 內記錄如下：

```md
# 計算學期科目成績調整0306

## 修改目的
調整 WearyDogComputer.cs 在「重修成績」規則下的重修成績來源判斷。
新增支援：當修課直接指定總成績有值時，優先使用指定總成績作為回寫重修成績來源，並仍受及格標準限制。

## 修改檔案
- SmartSchool.Evaluation.HS/WearyDogComputer.cs

## 調整前
- 重修成績來源僅使用 sacRecord.FinalScore
- 修課直接指定總成績不影響重修成績回寫

## 調整後
- 重修成績來源優先使用 designate_final_score
- 若無 designate_final_score，才使用 sacRecord.FinalScore
- 回寫重修成績時，仍以及格標準作為上限
- 保留高分覆蓋低分邏輯

## 驗證案例
- 課程成績 100、及格標準 60 → 重修成績 60
- 指定總成績 100、及格標準 60 → 重修成績 60
- 指定總成績 50、及格標準 60 → 重修成績 50
- 舊重修成績 60、新來源 55 → 維持 60
```
