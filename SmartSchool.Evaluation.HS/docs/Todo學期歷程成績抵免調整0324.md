# Cursor Todo學期歷程成績抵免調整0324.md

# 目標：修改抵免判斷規則

# 完成後記錄：學期歷程成績抵免調整0324.md

## 修改目標

在 `StudentLearningHistoryProcessor.cs` 中，調整 `抵免` 判斷規則：

* **保留**

  * `4.2 一般學期成績流程` 的抵免判斷：

    ```csharp
    if (Utility.GetAttribute(elmScore, "抵免") == "是")
        ssr.useCredit = "2";
    ```

* **註解掉**

  * `4.4 轉學轉科成績流程`
  * `6.2 一般學期成績流程`
  * `6.4 轉學轉科成績流程`

  內相同的抵免判斷。

## 注意事項

1. **不要修改其他邏輯**
2. **不要刪除程式，只註解**
3. **不要影響後續 `9D -> ssr.useCredit = "3"` 判斷**
4. **不要修改其他欄位計算**

   * `Score`
   * `ScoreP`
   * `ReScore`
   * `ReScoreP`
   * `CodePass`
   * `isScScore`
   * `StudType`
   * `YearScore`
   * 其他流程都不要動

---

## 修改檔案

`StudentLearningHistoryProcessor.cs`

---

## 一、保留的地方

### 4.2 一般學期成績流程

請保留這段，不要改：

```csharp
ssr.useCredit = "1";

if (Utility.GetAttribute(elmScore, "抵免") == "是")
    ssr.useCredit = "2";
```

---

## 二、要註解掉的地方

### 1. 4.4 轉學轉科成績流程

找到這段：

```csharp
ssr.useCredit = "1";
if (Utility.GetAttribute(elmScore, "抵免") == "是")
    ssr.useCredit = "2";
```

改成：

```csharp
ssr.useCredit = "1";
// if (Utility.GetAttribute(elmScore, "抵免") == "是")
//     ssr.useCredit = "2";
```

---

### 2. 6.2 一般學期成績流程

找到這段：

```csharp
ssr.useCredit = "1";
if (Utility.GetAttribute(elmScore, "抵免") == "是")
    ssr.useCredit = "2";
```

改成：

```csharp
ssr.useCredit = "1";
// if (Utility.GetAttribute(elmScore, "抵免") == "是")
//     ssr.useCredit = "2";
```

---

### 3. 6.4 轉學轉科成績流程

找到這段：

```csharp
ssr.useCredit = "1";
if (Utility.GetAttribute(elmScore, "抵免") == "是")
    ssr.useCredit = "2";
```

改成：

```csharp
ssr.useCredit = "1";
// if (Utility.GetAttribute(elmScore, "抵免") == "是")
//     ssr.useCredit = "2";
```

---

## 三、不要動的邏輯

以下邏輯務必保留原樣：

### 1. 9D 覆蓋判斷

這段不要改：

```csharp
if (!string.IsNullOrWhiteSpace(ssr.CourseCode))
{
    if (ssr.CourseCode.Length > 22)
    {
        string sub1 = ssr.CourseCode.Substring(startIndex1, endIndex);
        string sub2 = ssr.CourseCode.Substring(startIndex2, endIndex);
        if (sub1 == "9" && sub2 == "D")
        {
            ssr.useCredit = "3";
        }
    }
}
```

### 2. 不計學分排除

### 3. 是否補修成績判斷

### 4. 再次修習 / 重修 / 補修 既有流程

### 5. 4.2 / 4.4 / 6.2 / 6.4 的其他欄位邏輯

全部不要動

---

## 四、預期結果

修改完成後：

* `4.2`：仍保留抵免判斷，可寫出 `useCredit = "2"`
* `4.4`：不再因 `抵免 == "是"` 變成 `"2"`
* `6.2`：不再因 `抵免 == "是"` 變成 `"2"`
* `6.4`：不再因 `抵免 == "是"` 變成 `"2"`
* `9D` 課程若符合條件，仍可覆蓋成 `"3"`

---

## 五、驗證方式

### 情境 1：4.2 一般學期成績

資料：

* `抵免 = 是`
* 非 9D 課程

預期：

* `ssr.useCredit = "2"`

### 情境 2：4.4 轉學轉科成績

資料：

* `抵免 = 是`
* 非 9D 課程

預期：

* `ssr.useCredit` 維持 `"1"`

### 情境 3：6.2 一般學期成績

資料：

* `抵免 = 是`
* 非 9D 課程

預期：

* `ssr.useCredit` 維持 `"1"`

### 情境 4：6.4 轉學轉科成績

資料：

* `抵免 = 是`
* 非 9D 課程

預期：

* `ssr.useCredit` 維持 `"1"`

### 情境 5：任一流程為 9D 課程

資料：

* `CourseCode` 符合 `9D`

預期：

* `ssr.useCredit = "3"`

---

## 六、完成後紀錄

修改完成後，請記錄到：

`學期歷程成績抵免調整0324.md`

### 記錄內容至少包含

1. 修改目的

   * 僅保留 4.2 一般學期成績流程的抵免判斷
   * 註解掉 4.4 / 6.2 / 6.4 的抵免判斷

2. 修改檔案

   * `StudentLearningHistoryProcessor.cs`

3. 修改方式

   * 保留 4.2 原邏輯
   * 其餘 3 處僅註解，不刪除
   * 不影響 9D 覆蓋邏輯

4. 驗證結果

   * 4.2 仍可產生 `useCredit = "2"`
   * 4.4 / 6.2 / 6.4 不再因抵免變 `"2"`
   * 9D 仍可寫 `"3"`
