# 重讀成績處理檢查報告 - 2024/10/16

## 檢查檔案
`Process/Wizards/LearningHistory/StudentLearningHistoryProcessor.cs`

## 檢查目的
檢查重讀成績(5.3)的處理邏輯和相關功能

## 檢查結果摘要

### 重讀成績(5.3)處理流程

#### 1. 資料讀取階段
**位置**: `StudentLearningHistoryProcessor.cs` 第369行
```csharp
// 處理重讀回資料比對補考後回寫學習歷程重讀資料
// 讀取重讀資料
Dictionary<string, List<SubjectScoreRec108>> dataValue53 = GetLearningHistoryRetakeDataAsDictionary53(_SchoolYear, _Semester, studentIDList);
```

**說明**: 使用 `GetLearningHistoryRetakeDataAsDictionary53` 方法讀取重讀資料

#### 2. 學期成績比對階段
**位置**: `StudentLearningHistoryProcessor.cs` 第371-376行
```csharp
// 取得有重讀資料的學期成績
Dictionary<string, List<SubjectScoreRec108>> semsScore53 = new Dictionary<string, List<SubjectScoreRec108>>();
if (dataValue53.Count > 0)
{
    semsScore53 = GetStudentScoreDataAsDictionary(dataValue53.Keys.ToList());
}
```

**說明**: 根據重讀資料的學生ID取得對應的學期成績

#### 3. 補考成績比對階段
**位置**: `StudentLearningHistoryProcessor.cs` 第378-365行
```csharp
// 比對並填入補考成績資料
foreach (var studentData in dataValue53)
{
    string studentID = studentData.Key;
    List<SubjectScoreRec108> dataValueList = studentData.Value;

    // 檢查是否有對應的學期成績資料
    if (semsScore53.ContainsKey(studentID))
    {
        List<SubjectScoreRec108> semsScoreList = semsScore53[studentID];

        // 比對每個重讀記錄
        foreach (var dataValueRecord in dataValueList)
        {
            // 在學期成績中尋找對應的記錄
            var matchingSemsScore = semsScoreList.FirstOrDefault(s =>
                s.StudentID == dataValueRecord.StudentID &&
                s.SubjectName == dataValueRecord.SubjectName &&
                s.SubjectLevel == dataValueRecord.SubjectLevel &&
                s.SchoolYear == _SchoolYear.ToString() &&
                s.Semester == _Semester.ToString());

            if (matchingSemsScore != null)
            {
                // 填入補考成績
                dataValueRecord.ReScore = matchingSemsScore.ReScore;

                // 比較補考成績與及格標準
                if (!string.IsNullOrWhiteSpace(matchingSemsScore.ReScore))
                {
                    decimal scoreP = 60; // 預設及格標準
                    if (!string.IsNullOrWhiteSpace(matchingSemsScore.ScoreP))
                        decimal.TryParse(matchingSemsScore.ScoreP, out scoreP);

                    if (decimal.TryParse(matchingSemsScore.ReScore, out decimal reScore))
                    {
                        if (reScore >= scoreP)
                            dataValueRecord.ReScoreP = "1"; // 及格
                        else
                            dataValueRecord.ReScoreP = "0"; // 不及格
                    }
                    else
                    {
                        dataValueRecord.ReScoreP = "-1"; // 無法比較
                    }
                }
            }
            else
            {
                // 沒有找到對應的學期成績記錄
                dataValueRecord.ReScoreP = "-1"; // 預設值
            }
        }
    }
}
```

**說明**: 比對重讀資料與學期成績，填入補考成績並判斷及格狀態

#### 4. 資料寫入階段
**位置**: `StudentLearningHistoryProcessor.cs` 第2649-2661行
```csharp
// 使用 SaveScores53 方法回寫資料庫
if (allReScoreData53.Count > 0)
{
    try
    {
        _learningHistoryDataAccess.SaveScores53(allReScoreData53, _SchoolYear, _Semester);
    }
    catch (Exception ex)
    {
        FISCA.LogAgent.ApplicationLog.Log("重讀成績回寫", "錯誤", $"回寫失敗:{ex.Message}");
    }
}
```

**說明**: 將處理後的重讀資料回寫到資料庫

### 重讀成績資料結構

#### GetLearningHistoryRetakeDataAsDictionary53 方法
**位置**: `StudentLearningHistoryProcessor.cs` 第3007行
**功能**: 從 `student_learning_history` 表讀取重讀成績資料

**查詢條件**:
- `serial_no = '5.3'`
- `name = '重讀成績'`
- 指定學年度學期
- 指定學生ID列表

**資料欄位**:
- 身分證號、出生日期、課程代碼、開課年級、修課學分
- 再次修習成績、再次修習成績及格
- 補考成績、補考及格
- 重讀成績、成績及格、重讀註記
- 是否採計學分、質性文字描述
- 備註欄位（學生姓名、班級、座號、學號等）

### 重讀成績寫入方法

#### SaveScores53 方法
**位置**: `LearningHistoryDataAccess.cs` 第240行
**功能**: 將重讀成績資料寫入學習歷程

**寫入欄位對應**:
- `5.3.1` - 身分證號 (`ssr.IDNumber`)
- `5.3.2` - 出生日期 (`ssr.Birthday`)
- `5.3.3` - 課程代碼 (`ssr.CourseCode`)
- `5.3.4` - 科目名稱 (`ssr.SubjectName`)
- `5.3.5` - 開課年級 (`ssr.GradeYear`)
- `5.3.6` - 修課學分 (`ssr.Credit`)
- `5.3.7` - 再次修習成績 (`ssr.Score`)
- `5.3.8` - 再次修習成績及格 (`ssr.ScoreP`)
- `5.3.9` - 補考成績 (`ssr.ReScore`)
- `5.3.10` - 補考及格 (`ssr.ReScoreP`)
- `5.3.11` - 重讀成績 (`ssr.RepeatScore`)
- `5.3.12` - 成績及格 (`ssr.RepeatScoreP`)
- `5.3.13` - 重讀註記 (`ssr.RepeatMemo`)
- `5.3.14` - 是否採計學分 (`ssr.useCredit`)
- `5.3.15` - 質性文字描述 (`ssr.Text`)

### 重讀成績處理規則

#### 1. 再次修習處理
**位置**: `StudentLearningHistoryProcessor.cs` 第594-604行
```csharp
else if (rule == "重讀(擇優採計成績)" || rule == "再次修習")
    ruleType = "再次修習";

// 根據處理規則進行分支寫入
if (ruleType == "再次修習")
{
    // 再次修習不放入學期成績工作頁
    continue;
}
```

**說明**: 再次修習類型的成績不會放入學期成績工作頁

#### 2. 重讀註記處理
**位置**: `StudentLearningHistoryProcessor.cs` 第3091行
```csharp
rec.ReStudMark = dr["重讀註記"] + "";
```

**說明**: 重讀註記用於標示重讀類型（列抵請填1，再次修習請填2）

### 重讀成績專用欄位

#### SubjectScoreRec108 類別中的重讀專用欄位
- `RepeatScore`: 重讀成績（5.3 專用）
- `RepeatScoreP`: 重讀成績及格（1/0/-1，5.3 專用）
- `RepeatMemo`: 重讀註記（列抵請填1，再次修習請填2）
- `ReStudMark`: 重讀註記（舊版欄位）

### 處理邏輯特點

#### 1. 補考成績比對
- 重讀成績會與當學期學期成績進行比對
- 自動填入補考成績並判斷及格狀態
- 預設及格標準為60分

#### 2. 資料驗證
- 檢查資料完整性（身分證號、課程代碼必填）
- 驗證成績格式和數值範圍
- 錯誤處理和日誌記錄

#### 3. 批次處理
- 支援多學生批次處理
- 使用 Dictionary 結構提高查詢效率
- 異常處理確保資料完整性

### 重要發現

#### 1. 重讀成績處理流程
1. **資料讀取**: 從學習歷程表讀取重讀成績資料
2. **學期成績比對**: 取得對應學生的學期成績
3. **補考成績填入**: 比對並填入補考成績
4. **資料回寫**: 將處理後的資料回寫到學習歷程

#### 2. 重讀成績類型
- **列抵**: 重讀註記為 "1"
- **再次修習**: 重讀註記為 "2"

#### 3. 處理規則
- 再次修習不放入學期成績工作頁
- 補考成績會自動比對並填入
- 支援擇優採計成績的處理

### 結論

重讀成績(5.3)處理邏輯包含完整的資料讀取、比對、處理和回寫流程。系統會自動比對重讀資料與學期成績，填入補考成績並判斷及格狀態，最後將處理後的資料回寫到學習歷程表中。處理過程包含資料驗證、錯誤處理和日誌記錄，確保資料的完整性和正確性。
