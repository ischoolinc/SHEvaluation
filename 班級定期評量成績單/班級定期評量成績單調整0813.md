# Todo - 功能路徑調整

## 目的
將現有的功能按鈕  
`["資料統計"]["報表"]["成績相關報表"]["班級定期評量成績單(測試版)"]`  
修改為多新增一層 `即將下架報表`，變成：  
`["資料統計"]["報表"]["成績相關報表"]["即將下架報表"]["班級定期評量成績單(測試版)"]`

---

## Todo 1 - 調整功能路徑

### 步驟
1. 找到原本建立 RibbonBarItem 的程式碼：
   ```csharp
   var btn = K12.Presentation.NLDPanels.Class.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["班級定期評量成績單(測試版)"];
   ```
2. 修改為：
   ```csharp
   var btn = K12.Presentation.NLDPanels.Class.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["即將下架報表"]["班級定期評量成績單(測試版)"];
   ```
3. 確保 `btn.Enable` 判斷式與 `btn.Click` 事件綁定程式碼保持原樣：
   ```csharp
   btn.Enable = K12.Presentation.NLDPanels.Class.SelectedSource.Count > 0 &&
       FISCA.Permission.UserAcl.Current["0837256D-3C8B-49E6-8A71-F5E0D1182A46"].Executable;

   btn.Click += new EventHandler(Program_Click);
   ```

---

## Todo 2 - 確認權限設定與綁定
1. 保留原本的權限 GUID 與註冊程式碼：
   ```csharp
   RoleAclSource.Instance["班級"]["功能按鈕"].Add(
       new RibbonFeature("0837256D-3C8B-49E6-8A71-F5E0D1182A46", "班級定期評量成績單(測試版)")
   );
   ```
2. 不修改 GUID、不變更權限名稱。

---

## Todo 3 - 測試與驗證
1. 重新編譯專案並部署。
2. 啟動後確認 UI 菜單顯示路徑：
   ```
   資料統計 → 報表 → 成績相關報表 → 即將下架報表 → 班級定期評量成績單(測試版)
   ```
3. 確認：
   - 只有選取班級時按鈕才啟用。
   - 權限設定與之前一致。
   - 點擊按鈕後功能正常執行。

---

## 注意事項
- 僅修改功能路徑層級，不改變按鈕名稱與權限 GUID。
- 確認其他功能路徑不受影響。
- 若同一專案有多處相同路徑設定，需同步調整。

---

## Cursor 專用指令
**搜尋目標：**
```
K12.Presentation.NLDPanels.Class.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["班級定期評量成績單(測試版)"]
```

**修改指令：**
```
將 RibbonBarItems 的最後一層前插入 ["即將下架報表"]，變成：
K12.Presentation.NLDPanels.Class.RibbonBarItems["資料統計"]["報表"]["成績相關報表"]["即將下架報表"]["班級定期評量成績單(測試版)"]

保留後續的 Enable 與 Click 事件綁定程式碼原樣，不做其他改動。
```
